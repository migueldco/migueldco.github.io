<section class="section flex-center">
    <div class="container hero-content">
        <h1 class="animate-fade-in">
            Hi, I'm <span class="text-gradient">Miguel</span>. <br />
            Math and Computer Science enjoyer.
        </h1>
        <p class="subtitle animate-fade-in">
            I'm a computer science enthusiast with a deep love for mathematics
            and competitive programming. My work revolves around solving complex
            problems, from algorithmic challenges to CTFs and cybersecurity.
        </p>
        <div class="cta-buttons animate-fade-in">
            <a href="/projects" class="btn btn-primary">My Projects</a>
            <a href="/about" class="btn">About Me</a>
        </div>
    </div>
</section>

<style>
    .section {
        min-height: 80vh; /* Takes up most of the initial screen */
        display: flex;
        align-items: center;
    }

    .hero-content {
        max-width: 800px;
    }

    h1 {
        font-size: 4rem;
        line-height: 1.1;
        margin-bottom: 1.5rem;
    }

    .subtitle {
        font-size: 1.25rem;
        max-width: 600px;
        margin-bottom: 2.5rem;
        color: var(--color-text-muted);
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
    }

    /* Stagger animations */
    h1 {
        animation-delay: 0.1s;
        opacity: 0;
    }
    .subtitle {
        animation-delay: 0.2s;
        opacity: 0;
    }
    .cta-buttons {
        animation-delay: 0.3s;
        opacity: 0;
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem; /* Already reasonable, but maybe 2.2 for tighter fit */
            /* Let's actually verify if it looks good first, maybe keep as is or slightly smaller */
            font-size: 2.5rem;
        }

        .subtitle {
            font-size: 1.1rem;
        }

        .cta-buttons {
            flex-direction: column;
            width: 100%;
        }

        .btn {
            width: 100%;
            justify-content: center;
        }
    }

    @media (max-width: 350px) {
        h1 {
            font-size: 1.8rem;
        }
    }
</style>
